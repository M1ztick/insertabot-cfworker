/**
 * MCP tool definitions and dispatch have been removed.
 * Tool calling is now handled by the Cloudflare Agents SDK (AIChatAgent + this.mcp)
 * in src/lib/durable.ts. Connect MCP servers via the UI panel.
 */
